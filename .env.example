# Vorlagen Editor - Docker Environment Variables

# Database
DATABASE_URL=sqlite:////app/data/data.db

# Directory paths (must match Dockerfile)
TEMPLATE_DIR=/app/templates
TEMP_DIR=/app/temp
ARCHIVE_DIR=/app/archive

# Application settings
SESSION_LIFETIME_DAYS=5
TEMP_RETENTION_HOURS=48

# Optional: Disable Word COM converter (force docx2pdf fallback)
# DISABLE_WORD_CONVERTER=0

# Optional: FastAPI debug mode (set to 0 in production)
# DEBUG=0
